/**
 * Centralized app style tokens.
 * Change these values to update colors across the entire app.
 */

export const AppTheme = {
  /** Tint color for FAB icons on the map and active tab bar icons */
  accentColor: "#000",

  /** Inactive/muted icon color */
  mutedColor: "#999",
} as const;
